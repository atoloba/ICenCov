% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/icplots.R
\name{plotint}
\alias{plotint}
\title{Plot observed intervals}
\usage{
plotint(dt, order.by.y = FALSE, show.z = FALSE)
}
\arguments{
\item{dt}{A data frame with columns \code{zl}, \code{zr}. Optional columns:
\code{y} (used only when \code{order.by.y = TRUE}) and \code{z}
(used only when \code{show.z = TRUE}).}

\item{order.by.y}{Logical; if \code{TRUE}, order intervals by \code{y}
(ascending). If \code{FALSE}, order by the interval midpoint
\eqn{(zl + zr)/2} (default).}

\item{show.z}{Logical; if \code{TRUE}, plot the true \code{z} in blue.}
}
\value{
A \pkg{ggplot2} object.
}
\description{
Creates a plot from a dataframe containing left/right endpoints \code{zl},
\code{zr}. Optionally, points for the true covariate \code{z}
(e.g., in simulations) can be overlaid, and rows can be ordered by
an outcome \code{y}.
}
\examples{
data(carotenoids)

plotint(carotenoids) +
 labs(x=expression(paste("Total carotenoid concentration [", mu, "mol/L]")))


data(actg359)

plotint(actg359) +
 labs(x="Time since indinavir failure (weeks)")


}
