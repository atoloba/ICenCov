% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/internal_integration.R
\name{internal_integration}
\alias{internal_integration}
\alias{integration}
\title{Returns a n*m matrix with the integrals of \eqn{fun_i(s)} along the augmented
Turnbull intervals \eqn{I_j}, for \eqn{i=1,\dots,n} and \eqn{j=1,\dots,m}.}
\usage{
integration(
  fun = function(s, i, ...) dfam(y[i], thetHat[1] + thetHat[2] * s, thetHat[3], famly,
    size),
  intmap,
  Kappas,
  relTol = 1e-05,
  rel.tol = relTol
)
}
\arguments{
\item{fun}{Function of the form \code{fun(s, i, j, ...)} returning the
integrand value at scalar \code{s} for subject index \code{i} and interval
index \code{j}.}

\item{intmap, Kappas}{computed in gelc.fit}

\item{relTol}{Relative tolerance passed to \code{cubature::cubintegrate()} (default \code{1e-5}).}

\item{rel.tol}{Relative tolerance passed to \code{stats::integrate()} (default \code{relTol}).}
}
\value{
A list with components:
\describe{
\item{\code{results}}{Numeric \eqn{n \times m} matrix of integrals.}
\item{\code{errmat}}{Numeric \eqn{n \times m} matrix of absolute errors reported by the integrators.}
\item{\code{messmat}}{Character \eqn{n \times m} matrix of status messages (\code{"OK"}, \code{"maxEval reached"}, or \code{"error"}).}
\item{\code{warn}}{Character scalar with aggregated warnings (or \code{NA} if none).}
}
}
\description{
Numerical integration is attempted with \code{stats::integrate()} and, on failure,
retried with \code{cubature::cubintegrate()}.
}
\seealso{
\code{\link[stats]{integrate}}, \code{\link[cubature]{cubintegrate}}
}
\keyword{internal}
