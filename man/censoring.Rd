% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_interval_censoring.R
\name{censoring}
\alias{censoring}
\title{Simulate Non-Informative Censoring Intervals Around Latent Variable}
\usage{
censoring(z, zmin = 0, zmax = NULL, rini = NULL, rfun, dec = NULL)
}
\arguments{
\item{z}{Numeric vector. Latent true values.}

\item{zmin}{Numeric. Lower bound of the support for \code{z}. Default is 0.}

\item{zmax}{Optional numeric. Upper bound of the support for \code{z}. If \code{NULL}, only \code{zmin} is enforced.}

\item{rini}{Optional function. A function that generates the initial gap after \code{zmin}. If \code{NULL}, \code{rfun} is used instead.}

\item{rfun}{Function. Used to generate random positive gap widths.}

\item{dec}{Optional integer. Number of decimals to round the generated gaps. If \code{NULL}, no rounding is applied.}
}
\value{
A list with the following components:
\describe{
\item{zl}{Vector of left endpoints of the censoring interval.}
\item{zr}{Vector of right endpoints of the censoring interval.}
\item{gaps}{The full vector of simulated gap widths used to build the intervals.}
\item{warning}{Character string if any simulated interval fails to contain the true \code{z} value.}
}
}
\description{
Generates left (\code{zl}) and right (\code{zr}) interval endpoints for each value of a latent variable \code{z}.
The censoring mechanism is non-informative on the true value of the latent variable \code{z}.
}
\examples{
set.seed(1)
z <- runif(10, 0, 10)
censoring(z, rfun = function(n) rexp(n, 1), dec = 2)

}
\references{
Gómez, G., Calle, M. L., Oller, R., & Langohr, K. (2009).
Tutorial on methods for interval-censored data and their implementation in R.
\emph{Statistical Modelling}, 9(4), 259-297. \doi{10.1177/1471082X09009004}
}
